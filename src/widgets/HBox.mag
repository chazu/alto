namespace: 'Alto'

"HBox — horizontal layout container (compatibility wrapper).
 Delegates to BoxLayout horizontal. Prefer BoxLayout horizontal directly.
"

HBox subclass: BoxLayout

  classMethod: new [
    ^BoxLayout horizontal
  ]
