namespace: 'Alto'

"VBox — vertical layout container (compatibility wrapper).
 Delegates to BoxLayout vertical. Prefer BoxLayout vertical directly.
"

VBox subclass: BoxLayout

  classMethod: new [
    ^BoxLayout vertical
  ]
